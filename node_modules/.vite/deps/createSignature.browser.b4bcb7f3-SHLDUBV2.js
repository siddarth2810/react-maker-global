import "./chunk-WXXH56N5.js";

// node_modules/@uploadcare/rest-client/dist/esm/createSignature.browser.b4bcb7f3.mjs
var createSignature = async (secretKey, signString) => {
  const enc = new TextEncoder();
  const algorithm = { name: "HMAC", hash: "SHA-1" };
  const key = await crypto.subtle.importKey("raw", enc.encode(secretKey), algorithm, false, ["sign", "verify"]);
  const hashBuffer = await crypto.subtle.sign(algorithm.name, key, enc.encode(signString));
  const hashArray = Array.from(new Uint8Array(hashBuffer));
  const hashHex = hashArray.map((b) => b.toString(16).padStart(2, "0")).join("");
  return hashHex;
};
export {
  createSignature
};
//# sourceMappingURL=createSignature.browser.b4bcb7f3-SHLDUBV2.js.map
